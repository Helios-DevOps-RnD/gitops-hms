# ============================================================================
# PLACEHOLDER — Jenkins overwrites on every merge-to-main build.
# Sealed against the PROD cluster's cert (prod-pub.pem in Jenkins creds).
# See staging variant for the full explanation of this placeholder pattern.
# ============================================================================
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: hms-be-secret
  namespace: hms
spec:
  encryptedData: {}
  template:
    metadata:
      name: hms-be-secret
      namespace: hms
    type: Opaque
---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: hms-be-firebase
  namespace: hms
spec:
  encryptedData: {}
  template:
    metadata:
      name: hms-be-firebase
      namespace: hms
    type: Opaque
