# ============================================================================
# hms-pubsub — Express Node service (PG-backed event/queue handler).
# Image:  registry.hms.evanlwp.my.id/hms/pubsub:<git-sha>  (overlay sets the tag)
# Port:   3002
# Config sources (post-split):
#   - hms-pubsub-config  ConfigMap, non-secret runtime knobs (PGPORT/PGSSL/PGDATABASE)
#   - hms-pubsub-secret  SealedSecret per overlay, real PG credentials
#                        (PGHOST/PGUSER/PGPASSWORD only)
# ============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hms-pubsub
  namespace: hms
  labels:
    app.kubernetes.io/name: hms-pubsub
    app.kubernetes.io/part-of: hms
    app.kubernetes.io/component: pubsub
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: hms-pubsub
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: hms-pubsub
        app.kubernetes.io/part-of: hms
        app.kubernetes.io/component: pubsub
    spec:
      containers:
        - name: hms-pubsub
          image: registry.hms.evanlwp.my.id/hms/pubsub:placeholder
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3002
              protocol: TCP
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3002"
          envFrom:
            # Non-secret config: PGPORT, PGSSL, PGDATABASE
            - configMapRef:
                name: hms-pubsub-config
            # Real credentials: PGHOST, PGUSER, PGPASSWORD
            - secretRef:
                name: hms-pubsub-secret
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: hms-pubsub
  namespace: hms
  labels:
    app.kubernetes.io/name: hms-pubsub
    app.kubernetes.io/part-of: hms
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hms-pubsub
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
