# ============================================================================
# PLACEHOLDER — Jenkins overwrites this file on every production build from
# the `hms-pubsub-prod-config` Jenkins credential (key=value lines).
# See staging/configmap.yaml for key documentation.
# ============================================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: hms-pubsub-config
  namespace: hms
data:
  PGPORT: "5432"
  PGSSL: "true"
  PGDATABASE: "postgres"
